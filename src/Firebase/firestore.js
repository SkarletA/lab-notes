// // import React, { useState } from "react";

// import { collection, addDoc } from "../Firebase/firebase";
// import { db } from "../Firebase/firebaseconfig";

// export const saveData = (message, title) => addDoc(collection(db, 'blogs'),{
//   message,
//   title,
// });
